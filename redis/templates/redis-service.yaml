apiVersion: v1
kind: Service
metadata:
  name: redis-service
  namespace: {{ .Values.metadata.namespace }}
spec:
  selector:
    app: redis-cluster
  ports:
    - name: client
      protocol: TCP
      port: 6379
      targetPort: 6379
  sessionAffinity: None
  type: ClusterIP
